<UserControl x:Class="Presentation.WPF.Views.ContactListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="400">



    <Grid>
        <TextBlock Text="No contacts added yet..." FontSize="16" FontWeight="SemiBold" Foreground="Gray" 
                   HorizontalAlignment="Center" VerticalAlignment="Center">
            
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Contacts.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Contacts.Count}" Value="1">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>


        <ListView ItemsSource="{Binding Contacts}" Background="Transparent" VerticalAlignment="Stretch">
            <ListView.ItemTemplate>
                <DataTemplate>

                    <StackPanel Orientation="Horizontal" Margin="5" VerticalAlignment="Center">

                        <StackPanel Orientation="Vertical" Margin="0 0 10 0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding FirstName}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding LastName}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Email}" Foreground="Gray" FontSize="12"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="10 0 0 0" VerticalAlignment="Center">
                            <Button Content="✏️" Width="30" Height="30" Margin="10 0 0 0" 
                                    VerticalAlignment="Center"
                                    Command="{Binding DataContext.EditContactCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                    CommandParameter="{Binding}" />
                            <Button Content="❌" Width="30" Height="30" Margin="10 0 0 0"
                                    VerticalAlignment="Center"
                                    Command="{Binding DataContext.DeleteContactCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                    CommandParameter="{Binding}" />
                        </StackPanel>
                        
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>



        <Button Content="Add Contact"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Margin="10 30"
                Width="150"
                Height="40"
                Click="AddContactButton_Click" />

    </Grid>

</UserControl>
