<UserControl x:Class="Presentation.WPF.Views.ContactListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="400">



    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image Source="pack://application:,,,/Resources/empty.png" Width="150" Height="150" />
            <TextBlock Text="No contacts found" FontSize="16" FontWeight="SemiBold" Foreground="Gray" Visibility="Hidden">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Contacts.Count}" Value="0">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>


        <ListView ItemsSource="{Binding Contacts}" Background="Transparent"
                  Visibility="Visible">
            <ListView.Style>
                <Style TargetType="ListView">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Contacts.Count}" Value="0">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.Style>
        </ListView>
        
        

        <Button Content="Add Contact"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Margin="10 30"
                Width="150"
                Height="40"
                Click="AddContactButton_Click" />

    </Grid>

</UserControl>
