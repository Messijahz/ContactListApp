<UserControl x:Class="Presentation.WPF.Views.ContactListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="400">
    
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="10">
        <StackPanel>

            <TextBlock Text="Contact List" FontSize="24" FontWeight="Bold"  Margin="10" HorizontalAlignment="Center" />
            
            
            <ListView ItemsSource="{Binding Contacts}" Background="Transparent">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border  Margin="5" Background="White" BorderBrush="Gray" BorderThickness="1" Padding="10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="50"/>
                                </Grid.ColumnDefinitions>

                                <Button Content="▼" 
                                        Grid.Column="0" 
                                        Width="30" 
                                        Height="30"
                                        Command="{Binding DataContext.ToggleExpandCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding}" />

                                <StackPanel Grid.Column="1" Orientation="Vertical">
                                    <TextBlock Text="{Binding FirstName}" FontSize="16" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Email}" FontSize="14" Foreground="Gray" />
                                </StackPanel>

                                <Button Content="✎"
                                        Grid.Column="2"
                                        Width="30"
                                        Height="30"
                                        Command="{Binding DataContext.EditContactCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding}" />

                                <Button Content="✖"
                                        Grid.Column="3"
                                        Width="30"
                                        Height="30"
                                        Command="{Binding DataContext.DeleteContactCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding}" />
                            
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackPanel>        
    </ScrollViewer>
    
</UserControl>
